import TokenBalance from "../components/TokenBalance";

const SmartContract = () => {
  return (
    <section>
      SmartContract
      <TokenBalance />
    </section>
  );
};

export default SmartContract;
